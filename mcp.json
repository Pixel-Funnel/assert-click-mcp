{
  "name": "Assert",
  "description": "Run real Playwright E2E tests from your AI coding agent. Describe what to test in plain English, Assert writes and executes the test, and reports back step-by-step results.",
  "icon": "https://assert.click/img/icon.png",
  "homepage": "https://assert.click",
  "server": {
    "command": "npx",
    "args": ["-y", "@assert-click/mcp"],
    "env": {
      "ASSERT_API_KEY": {
        "description": "Your Assert API key. Get one free at dashboard.assert.click",
        "required": true
      }
    }
  }
}
